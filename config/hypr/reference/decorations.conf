# ── Flat Nothing-style Decorations ─────────────────────────────────────
decoration {
  rounding = 4

  drop_shadow = false

  blur {
    enabled = false
  }

  active_opacity = 1.0
  inactive_opacity = 1.0
  fullscreen_opacity = 1.0
}
